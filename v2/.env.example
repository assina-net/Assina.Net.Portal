# Conexão Postgres (piloto: RDS compartilhado, schema assinanet)
# valor real em: pass assina.net/rds
DATABASE_URL="postgresql://USER:PASS@HOST:5432/zeroaudio?schema=assinanet&sslmode=require"

# Auth.js — segredo de sessão (gerar com: openssl rand -base64 32)
AUTH_SECRET="changeme"
# URL base da app (dev)
AUTH_URL="http://localhost:3000"
